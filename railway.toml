# Railway deployment configuration (root)
# Each service reads its own railway.toml from its Root Directory:
#   - Vidya-AI API  → Root Dir: apps/api → apps/api/railway.toml
#   - Vidya-AI App  → Root Dir: (repo root) → this file or apps/web/railway.toml
# This file is intentionally minimal to avoid overriding service configs.
