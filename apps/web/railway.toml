# Railway deployment configuration for Vidya Web App
# Configure in Railway dashboard (not via this file):
#   Root Directory: / (blank / repo root)
#   Builder: Dockerfile
#   Dockerfile Path: Dockerfile.web
#   Start Command: (leave blank, uses Dockerfile CMD)
#   Healthcheck: /
#   Build arg: VITE_API_URL = https://vidya-ai-production.up.railway.app
