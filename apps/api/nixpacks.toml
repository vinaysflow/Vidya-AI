[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npx prisma generate --schema prisma/schema.prisma", "npx tsc"]

[start]
cmd = "node dist/index.js"
